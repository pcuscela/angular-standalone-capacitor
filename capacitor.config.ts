import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'it.angularstandalonecapacitor.app',
	appName: 'angular-standalone-capacitor',
	webDir: 'dist/browser',
};

export default config;
